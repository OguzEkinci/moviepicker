import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    mainTitle:{
        color: '#191919',
        fontSize: 40,
        fontWeight: 'bold'
    },
    subTitle: {
        color: '#484848',
        fontSize: 17,
    }
})
export {styles}